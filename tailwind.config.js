/** @type {import('tailwindcss').Config} */
export default {
  // Specify files to scan for class names
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  
  // Theme customization
  theme: {
    extend: {}, // Extend default theme
  },
  
  // Additional plugins
  plugins: [], // No plugins currently used
};
